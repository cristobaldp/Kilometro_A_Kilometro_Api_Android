<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 INTERNET (FastAPI) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 📍 UBICACIÓN (Google Maps) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kilometro"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 🔑 GOOGLE MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBpqWe4XyhE9WLvzmbfPmKi2JsvG6jPsRQ"/>

        <!-- LOGIN (Launcher) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.register.RegisterActivity"/>
        <activity android:name=".ui.menu.MenuPrincipalActivity"/>
        <activity android:name=".ui.vehiculos.VehiculosActivity"/>
        <activity android:name=".ui.vehiculos.AddVehiculoActivity"/>
        <activity android:name=".ui.repostajes.RepostajesActivity"/>
        <activity android:name=".ui.repostajes.AddRepostajeActivity"/>
        <activity android:name=".ui.estadisticas.EstadisticasActivity"/>
        <activity android:name=".ui.comparar.CompararVehiculosActivity"/>
        <activity android:name=".ui.ajustes.AjustesActivity"/>
        <activity android:name=".ui.mapa.MapaGasolinerasActivity"/>

    </application>

</manifest>
